0.1.9 性能补丁：缩短模式切换和首轮对话等待。

- 模式切换不再同步扫描整个工程目录，先完成界面切换，工程扫描按需执行。
- 快照读取不再启动 MCP、等待 CODESYS Bridge 或逐个连接外部服务。
- MCP 工具改为按需并行发现，并按模式和服务配置缓存，后续对话复用工具目录。
- 启动时模型配置与工作区快照并行读取，移除重复的模型配置加载。
- `/mcp`、`/tools` 和显式工具列表统一使用缓存，避免重复握手。

0.1.8 小版本更新：按 Codex 风格修正 PLC Pilot 品牌标识。

- 侧边栏不再显示独立的 P 图标，改为单一的 PLC Pilot 品牌组合。
- Pilot 使用橙色底纹；明亮主题使用黑色文字，暗黑主题使用白色文字。
- 保留工作模式下拉、搜索和新建会话入口的位置与行为不变。

0.1.7 功能更新：增加 CODESYS、自由聊天、Stone 三种工作模式。

- 左上角新增类似 Codex 的工作模式下拉框：codesys、自由聊天、Stone。
- CODESYS 模式保留工程概览、CODESYS Bridge 和 PLC 工具。
- 自由聊天模式移除 CODESYS/Stone 专用上下文，可处理通用问答、文件和编程。
- Stone 模式自动启用内置 CAREL STone MCP，使用官方 API 参考和工程工具。
- 设置页“返回对话”移到左侧导航底部，与 Codex 设置布局一致。
- Logo、窗口图标和 favicon 改为黑/白/橙的简洁高对比样式。

0.1.6 补丁更新：修复无法添加多个模型服务商的问题。

- 模型设置新增独立的“添加服务商”：名称、URL、接口类型和 Key 可先保存，无需先填写模型。
- 支持同时启用多个服务商，在各自详情中获取、勾选添加模型或手动填写模型 ID。
- 服务商开关独立于模型勾选，停用再启用不会丢失模型选择；同名模型按服务商独立保存和切换。
- 旧模型配置自动迁移至对应服务商，保留模型 ID、上下文、思考等级和本机凭据。
- 获取结果绑定当前服务商；切换服务商或修改连接后清除旧结果，防止模型导入错误分组。
- 对话模型选择器显示所属服务商。服务商 URL、Key 与协议统一配置，模型保留独立上下文与输出限制。
- 保留 STone MCP 和本机诊断日志；日志位置为 `%LOCALAPPDATA%\PLC Pilot\logs`。

参考 cc-switch 的独立服务商数据结构与新增表单职责，复用 PLC Pilot 既有加密凭据和原子配置保存。

同事电脑测试出现问题时，请压缩上述 logs 文件夹，并附上复现时间、操作步骤和 STone 版本；不需要复制 auth.json 或模型配置文件。本机未安装 STone，本版本未宣称通过真实工程编译和设备验收。

在“设置 → 软件更新”或“帮助 → 检查更新”中可检查并安装本版本。

安装版与便携版均包含 Node.js、npm/npx、Python 和 pip。更新会先保存草稿与会话，等待任务结束，再校验签名并替换程序。便携版替换遇到问题时恢复旧程序；模型、密钥、项目、MCP/Skills 配置和聊天记录保留。

推荐一般用户下载安装包 `PLC-Pilot-0.1.6-x64-setup.exe`；便携用户下载 ZIP 后完整解压。便携版需要 Windows 已安装 WebView2，安装版可联网补装。

0.1.0 尚未内置更新功能，需要先手动安装一次本版本。之后可直接在软件内更新。更新签名用于校验发布文件；Windows SmartScreen 仍可能提示未签署 Windows 代码签名。
