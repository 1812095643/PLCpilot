0.1.17 会话中心与项目上下文更新：补齐全局会话检索、项目记忆管理和项目知识库。

- 侧边栏新增“会话中心”，支持搜索本机保存会话的标题、工作目录和正文，并按项目筛选。
- 会话中心支持恢复、重命名和删除历史会话，继续复用原有 JSONL 会话和分支机制。
- 新增“项目上下文”页面，分为项目记忆和项目知识库；支持新增、编辑、删除和导入 Markdown、文本、代码及结构化文本文件。
- 项目记忆继续使用 Codex/Pi 风格的项目隔离仓库和 Agent `memory` 工具；项目知识条目保留更长内容并不按普通记忆的 90 天规则过期。
- 修复总耗时在工具、命令、重试或审批后重新计时的问题；整轮耗时从用户发送开始持续到任务结束，并写入会话记录，历史恢复后仍可显示。
- 保留上一版本的 HTML 隔离预览、Office 文档工作区和随包运行时。

0.1.16 HTML 文件预览与文档工作区更新：支持安全查看 `.html` / `.htm` 文件。

- HTML 文件会在右侧文件工作区中自动渲染预览，也可以切换到源码视图并引用选中文本。
- 预览使用隔离 iframe 和内容安全策略，禁用脚本、表单、嵌入页面、页面跳转及事件属性，避免把本地 HTML 当作可执行页面运行。
- 文档核心新增 HTML 分类、正文读取、只读状态和安全提示测试；继续保留 Word、Excel、PPT、PDF、图片和代码文件预览能力。
- 本版本同步包含上一阶段的 Office 文档工作区、随包运行时和审批继续机制改动。

0.1.15 新增中国专利 Skill：内置 `handsomestWei/patent-disclosure-skill` 完整资源。

- Skills 设置页新增“中国专利交底与申请”，默认随 PLC Pilot 提供，可在自由聊天、CODESYS 和 Stone 模式中按需选择。
- 完整保留上游 9 个子技能、提示词、参考资料、文档生成/检索工具和 MIT License；入口支持交底书、申请文件、案卷会稿、专利检索、通俗解读、专利地图、政策简报和审查答复。
- 针对随包嵌入式 Python 补齐上游工具缺少的共享 UTF-8 模块，并让浏览器/公式脚本显式定位自身工具目录，确保空电脑直接运行。
- 安装包把完整 Skill 目录放入程序旁的 `skills\patent-disclosure-skill` 资源目录，生成 Word、线稿和检索脚本直接使用包内路径，不依赖源码目录或临时文件夹。
- 专利 Skill 使用随包 Python 运行时，核心 Word/PPTX、YAML、公式和浏览器自动化依赖随包提供；PDF 专用、CAD、matplotlib、浏览器内核和向量模型仍按需安装。
- 上游来源：`https://github.com/handsomestWei/patent-disclosure-skill`，本次同步提交：`39d96b8be4afd19f936f66a995c6b4ae28378915`。

在“设置 → 软件更新”或“帮助 → 检查更新”中可检查并安装本版本。更新保留模型、密钥、项目、MCP/Skills 配置和聊天记录。

- 安装版：`PLC-Pilot-0.1.15-x64-setup.exe`，同时提供 MSI 安装包。
- 便携版：`PLC-Pilot-Portable-0.1.15-win-x64.zip`，请完整解压，保留运行时目录。
- 安装完成页继续提供立即启动、桌面快捷方式与开机自启选项；设置内可管理开机自启。
- 安装版和便携版均包含 Node.js、npm/npx、Python 和 pip，继续提供独立的 STone MCP 压缩包。

便携版需要 Windows 已安装 WebView2；安装版可联网补装。发布文件提供更新签名和 SHA-256 校验清单。

0.1.14 会话交互与界面更新：修复文件拖拽和外链打开，完善欢迎页与工具目录。

- 点击侧边栏项目名称仅展开或折叠，点击具体会话才打开对话。
- 任务运行时输入框只保留停止按钮；Enter 排队，Ctrl+Enter 立即调整方向，Shift+Enter 换行。
- 拖入文件夹自动打开为项目；图片、Excel 和其他单文件添加到输入框作为附件，保留已有草稿。
- 修复 GitHub、全部版本与安装包等链接无法打开的问题，统一使用系统默认浏览器；对话中的本地文件链接使用默认应用打开。
- 工具目录默认每个工具显示一行，点击或按 Enter 展开、折叠详情，避免长说明铺满页面。
- 欢迎页使用 PLC Pilot 文字 Logo，四个提问入口分别使用不同颜色的文字与图标，收窄、加高并下移。
- 修复欢迎页暗色主题文字对比度，Logo、标题、说明与彩色入口跟随全局主题。
- “帮助 → 打赏作者”加入支付宝图片。

在“设置 → 软件更新”或“帮助 → 检查更新”中可检查并安装本版本。更新保留模型、密钥、项目、MCP/Skills 配置和聊天记录。

- 安装版：`PLC-Pilot-0.1.14-x64-setup.exe`，同时提供 MSI 安装包。
- 便携版：`PLC-Pilot-Portable-0.1.14-win-x64.zip`，请完整解压，保留运行时目录。
- 安装完成页继续提供立即启动、桌面快捷方式与开机自启选项；设置内可管理开机自启。
- 安装版和便携版均包含 Node.js、npm/npx、Python 和 pip，继续提供独立的 STone MCP 压缩包。

便携版需要 Windows 已安装 WebView2；安装版可联网补装。发布文件提供更新签名和 SHA-256 校验清单。
