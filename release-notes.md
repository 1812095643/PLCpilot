0.1.4 补丁更新：右键菜单根据实际链接类型显示对应操作，全部使用中文。

- 本地文件和目录：用默认应用打开、在资源管理器中显示、复制路径。
- HTTP/HTTPS 网址（包括安装包、ZIP 下载链接）：在浏览器中打开、复制链接。
- 修复外链因路径类似本地地址而出现错误菜单的问题，保留中文、空格及 # 等文件名字符。
- 删除无效的内部编辑入口；桌面端网址由系统默认浏览器打开。

在“设置 → 软件更新”或“帮助 → 检查更新”中可检查并安装本版本。

安装版与便携版均包含 Node.js、npm/npx、Python 和 pip。更新会先保存草稿与会话，等待任务结束，再校验签名并替换程序。便携版替换遇到问题时恢复旧程序；模型、密钥、项目、MCP/Skills 配置和聊天记录保留。

推荐一般用户下载安装包 `PLC-Pilot-0.1.4-x64-setup.exe`；便携用户下载 ZIP 后完整解压。便携版需要 Windows 已安装 WebView2，安装版可联网补装。

0.1.0 尚未内置更新功能，需要先手动安装一次本版本。之后可直接在软件内更新。更新签名用于校验发布文件；Windows SmartScreen 仍可能提示未签署 Windows 代码签名。
