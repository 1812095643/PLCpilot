0.1.11 体验更新：品牌开屏、会话命名与预览提速、模型元数据同步。

- 新增跟随明暗主题的 PLC Pilot 开屏：PLC 字母依次弹跳，落地时轻微变扁变宽，回弹形变逐次衰减；随后向右展开橙底 Pilot。
- 开屏保留已确认的动画速度，完整播放后停留 0.5 秒再进入工作台；初始化与动画并行，支持系统减少动态效果，并保留超时退出保护。
- 临时会话默认显示“新对话”，首轮回复完成后根据提问生成简短标题并保存；只附加文件时使用附件名称生成标题，磁盘目录仍保留唯一标识。
- 会话 JSONL 预览按文件路径、修改时间和大小缓存，减少刷新历史列表时的重复读取与解析；文件更新、改名或删除后自动刷新缓存。
- 服务商获取模型时，如接口提供上下文长度、最大输出和思考档位，会在列表中展示并随勾选导入一起保存。
- 空白对话页新增与 CODESYS、自由聊天、Stone 模式匹配的功能介绍和推荐问题，点击后填入输入框，保留已有草稿。
- 回复结束后减少重复读取模型配置；无工程、非 CODESYS 模式或正在回复时跳过无效的周期同步。

在“设置 → 软件更新”或“帮助 → 检查更新”中可检查并安装本版本。更新保留模型、密钥、项目、MCP/Skills 配置和聊天记录。

- 安装版：`PLC-Pilot-0.1.11-x64-setup.exe`，同时提供 MSI 安装包。
- 便携版：`PLC-Pilot-Portable-0.1.11-win-x64.zip`，请完整解压，保留运行时目录。
- 安装版和便携版均包含 Node.js、npm/npx、Python 和 pip，继续提供独立的 STone MCP 压缩包。

便携版需要 Windows 已安装 WebView2；安装版可联网补装。发布文件提供更新签名和 SHA-256 校验清单。
