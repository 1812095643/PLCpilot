0.1.5 测试更新：内置 CAREL STone MCP，并加入无需页面入口的本机诊断日志。

- MCP 商店新增 CAREL STone（内置）：53 个工具，覆盖 33 个官方 IronPython 方法、8 个 CLI 入口，以及完整官方属性与 API 参考。
- 完整收录 1,138 个控制器 ST 库条目、72 个帮助页和 54 个库页面；无需安装 STone 即可离线检索资料。
- 工程自动化需在测试电脑安装 CAREL STone 及相应许可；自动探测安装位置，或在 MCP 环境变量填写 STONE_CLI_PATH。
- 日志默认保存至 `%LOCALAPPDATA%\PLC Pilot\logs`，记录软件启动/异常、模型重试与流式状态、MCP 握手和工具调用、SToneCLI 输出、退出码、中断与超时。
- 日志包含版本、时间、进程和请求编号，自动过滤凭据并轮转；不增加任何页面入口。目录不可写时回退到 `%TEMP%\PLC Pilot\logs`。
- 修复 MCP 会话首次调用死锁，以及打包后重复启动入口引起的工具列表异常。

同事电脑测试出现问题时，请压缩上述 logs 文件夹，并附上复现时间、操作步骤和 STone 版本；不需要复制 auth.json 或模型配置文件。本机未安装 STone，本版本未宣称通过真实工程编译和设备验收。

在“设置 → 软件更新”或“帮助 → 检查更新”中可检查并安装本版本。

安装版与便携版均包含 Node.js、npm/npx、Python 和 pip。更新会先保存草稿与会话，等待任务结束，再校验签名并替换程序。便携版替换遇到问题时恢复旧程序；模型、密钥、项目、MCP/Skills 配置和聊天记录保留。

推荐一般用户下载安装包 `PLC-Pilot-0.1.5-x64-setup.exe`；便携用户下载 ZIP 后完整解压。便携版需要 Windows 已安装 WebView2，安装版可联网补装。

0.1.0 尚未内置更新功能，需要先手动安装一次本版本。之后可直接在软件内更新。更新签名用于校验发布文件；Windows SmartScreen 仍可能提示未签署 Windows 代码签名。
